# Copy to .env and fill in. .env is gitignored — never commit real keys.
METSERVICE_TIDE_KEY=your-beta-key-here

# Optional overrides:
# METSERVICE_TIDE_BASE_URL=https://test-api.marine.metservice.com/tide/v4
# METSERVICE_TIDE_TIMEOUT_MS=20000
